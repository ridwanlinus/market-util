# Files in this directory are generated by `composer install`
# and should not be committed. However, on serverless platforms
# (Vercel), the builder may not regenerate them, so we track
# the essential ones.
*.pid
*.tmp
